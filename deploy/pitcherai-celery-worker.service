[Unit]
Description=PitcherAI Celery Worker
After=network.target postgresql.service redis.service pitcherai-web.service
Wants=postgresql.service redis.service pitcherai-web.service

[Service]
Type=simple
User=pitcherai
Group=pitcherai
WorkingDirectory=/opt/pitcherai
Environment="PATH=/opt/pitcherai/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
EnvironmentFile=/opt/pitcherai/.env
ExecStart=/opt/pitcherai/.venv/bin/celery -A pitcherai.tasks worker --loglevel=info --concurrency=4
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
